#include <stdio.h>
int main()
{
    float x;
    scanf("%f", &x);
int ans = 
    if (x == .00)
    {
        printf("int %.0f", x);
    }
    else
    {
        int ans = x;
        printf("float %d %.3f ", ans, x - ans);
    }

    return 0;
}